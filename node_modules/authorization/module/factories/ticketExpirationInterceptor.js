/**
 * Created by Panin.D on 2/7/14.
 */

angular
    .module('whatwanted.AuthorizationModule')
    .factory('ticketExpirationInterceptor', ['$window', '$q', function (window, $q) {

        return {
            'responseError': function (response) {

                if (response.status === 401) {
                    window.location.reload();
                }
                else return $q.reject(response);
            }
        };
    }]);